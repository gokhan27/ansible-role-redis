---
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

- name: Install the redis components
  hosts: redis_all
  gather_facts: "{{ osa_gather_facts | default(True) }}"
  max_fail_percentage: 20
  user: root
  pre_tasks:
    - include: common-tasks/os-lxc-container-setup.yml
    - include: common-tasks/rabbitmq-vhost-user.yml
      static: no
      vars:
        user: "{{ redis_rabbitmq_userid }}"
        password: "{{ redis_rabbitmq_password }}"
        vhost: "{{ redis_rabbitmq_vhost }}"
        _rabbitmq_host_group: "{{ redis_rabbitmq_host_group }}"
      when:
        - inventory_hostname == groups['redis_api'][0]
        - groups[redis_rabbitmq_host_group] | length > 0
    - include: common-tasks/os-log-dir-setup.yml
      vars:
        log_dirs:
          - src: "/openstack/log/{{ inventory_hostname }}-redis"
            dest: "/var/log/redis"
    - include: common-tasks/mysql-db-user.yml
      static: no
      vars:
        user_name: "{{ redis_galera_user }}"
        password: "{{ redis_container_db_password }}"
        login_host: "{{ redis_galera_address }}"
        db_name: "{{ redis_galera_database }}"
      when: inventory_hostname == groups['redis_all'][0]
    - include: common-tasks/package-cache-proxy.yml
  roles:
    - role: "os_redis"
    - role: "openstack_openrc"
      tags:
        - openrc
    - role: "rsyslog_client"
      rsyslog_client_log_rotate_file: redis_log_rotate
      rsyslog_client_log_dir: "/var/log/redis"
      rsyslog_client_config_name: "99-redis-rsyslog-client.conf"
      tags:
        - rsyslog
  vars:
    is_metal: "{{ properties.is_metal|default(false) }}"
    redis_rabbitmq_userid: redis
    redis_rabbitmq_vhost: /redis
    redis_rabbitmq_servers: "{{ rabbitmq_servers }}"
    redis_rabbitmq_port: "{{ rabbitmq_port }}"
    redis_rabbitmq_use_ssl: "{{ rabbitmq_use_ssl }}"
  environment: "{{ deployment_environment_variables | default({}) }}"
  tags:
    - redis
